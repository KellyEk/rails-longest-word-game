<h1>New game</h1>
<p><%= @letters %><br/>
<form action="/score" method="post" data-turbo="false">
  <!-- ... -->
  <input type="text" class="form-control-sm" name="word">
    <button type="submit" class="btn btn-primary" value="<%= params[:word] %>">Play</button>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= hidden_field_tag :letters, @letters %>
</form>
